akka {
    zeromq {

        poll-timeout = 1ms

        new-socket-timeout = 10s

        socket-dispatcher {
            executor = thread-pool-executor
            type = PinnedDispatcher
        }
    }
}
